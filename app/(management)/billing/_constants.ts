export const MANAGEMENT_NOTIFICATION_ROUTE = "/(modals)/admin-notifications";

export const METER_READING_STATUS_OPTIONS = ["all", "pending", "verified", "rejected"] as const;

export const BILL_STATUS_OPTIONS = ["all", "pending", "partial", "paid", "overdue"] as const;
