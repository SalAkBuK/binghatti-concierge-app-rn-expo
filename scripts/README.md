# Scripts

This directory contains repository utility scripts and local helper tools.

Current layout:

- `windows/` for Windows batch helpers such as local APK build shortcuts
- `dev/` for local development utilities such as OAuth callback helpers

Keep ad hoc helper scripts here instead of the repo root.
